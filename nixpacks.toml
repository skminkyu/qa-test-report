[phases.install]
cmds = ["npm install", "npm install --prefix backend", "npm install --prefix frontend"]

[phases.build]
cmds = ["npm run build --prefix frontend", "npm run build --prefix backend"]

[start]
cmd = "node backend/dist/index.js"
